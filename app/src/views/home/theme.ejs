<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>테마 설정</title>
    <link rel="stylesheet" href="/css/home/theme.css"> <!-- 테마 스타일 연결 -->
</head>
<body>
    <h1>테마 저장</h1>
    
    <form id="theme-form">
        <label for="theme_name">테마 이름:</label>
        <input type="text" id="theme_name" name="theme_name" required>
    
        <label for="theme_code">테마 코드:</label>
        <input type="text" id="theme_code" name="theme_code" required 
               pattern="\d{4}" maxlength="4" title="숫자 4자리만 입력 가능" 
               oninput="this.value = this.value.replace(/[^0-9]/g, '').slice(0,4)">

        <label for="escape_time_minutes">탈출 제한 시간 (분):</label>
        <input type="text" id="escape_time_minutes" name="escape_time_minutes" required 
               pattern="\d{1,2}" maxlength="2" title="숫자 2자리만 입력 가능 (0~99)" 
               oninput="this.value = this.value.replace(/[^0-9]/g, '').slice(0,2)">

        <label for="available_hints">사용 가능 힌트 수:</label>
        <input type="text" id="available_hints" name="available_hints" required 
               pattern="\d" maxlength="1" title="숫자 1자리만 입력 가능 (0~9)" 
               oninput="this.value = this.value.replace(/[^0-9]/g, '').slice(0,1)">

        <label for="monitoring_places">모니터링 장소 개수:</label>
        <input type="text" id="monitoring_places" name="monitoring_places" required 
               pattern="\d" maxlength="1" title="숫자 1자리만 입력 가능 (0~9)" 
               oninput="this.value = this.value.replace(/[^0-9]/g, '').slice(0,1)">
        
        <!-- 🔹 맵 추가 (파일 업로드 버튼) -->
        <button id="add-map-btn" type="button">맵 추가</button>
        <input type="file" id="map-upload" accept=".jpg, .pdf" style="display: none;" multiple>

        <!-- 🔹 업로드된 파일 목록 -->
        <ul id="file-list"></ul>

        <button type="submit">테마 저장</button>
    </form>

    <script src="/js/home/theme.js"></script> <!-- 테마 저장 기능 연결 -->
</body>
</html>
